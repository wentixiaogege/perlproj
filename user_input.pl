#!/usr/bin/perl

# getting user input
$user_input = <>;
# also possible to use just (<>)
print $user_input;
# clear user input and remove new line character
chomp($user_input);
print $user_input ." "; 